import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const VaccineList = () => {
  return (
    <SafeAreaView>
      <Text>VaccineList</Text>
    </SafeAreaView>
  );
};

export default VaccineList;
